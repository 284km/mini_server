require 'spec_helper'

describe MiniServer do
  it 'has a version number' do
    expect(MiniServer::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
